# Server Action

record.write({
    'x_studio_status': 'picked_up',
    'x_studio_delivered_by': env.user.id,
})
record.message_post(
    body=f"Package marked as Picked up/Delivered by {env.user.name}.",
    message_type="comment",
    subtype_xmlid="mail.mt_note",
)
